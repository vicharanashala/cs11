# Memory — 2026-06-01

## Session Summary

Third session with Emie on the CrowdFAQ project. Focused on auditing and verifying all 25 previously identified frontend issues across the full frontend codebase.

## Frontend Audit — All 25 Issues Reviewed

TypeScript: `npx tsc --noEmit` passes cleanly (exit 0). Zero type errors.

### Issues Status

**22 ✅ Fixed:**
- #1 `_id`/`id` mismatch → `AiSuggestionBanner` now uses `id` field
- #2 QuestionForm double-submit → local mutation only created when no injected mutation
- #3 CategoryFilter always-false → uses `useSearch` + proper `activeCategory === cat.slug` comparison
- #4 Pagination `total` → `totalCount` → `total` mapping in `useFaqs.fetchFaqs`
- #5 questions.tsx wrong param → uses `user._id` (MongoDB ObjectId) correctly
- #6 `user.id` vs `user._id` → `AuthContext` exposes `_id` throughout
- #7 `search: {}` with `as any` → clean `navigate({ to: '/faqs' })`
- #8 `pendingPayload` wrong value → set to original form payload via `onAiMatch` callback
- #9 Hardcoded `/faqs` → `SearchBar` + `CategoryFilter` accept `baseRoute` prop
- #10 Negative vote score → green/red/grey colour coding in `AnswerCard`
- #11 `login` async/await → fixed with try/finally
- #12 SubmitAnswerForm error not cleared → `setError(null)` before mutation
- #13 QuestionForm error not cleared → `setApiError(null)` before mutation
- #14 Status badge unstyled → full `STATUS_STYLES` map in `admin.queries.tsx`
- #15 `faq.category` → `[object Object]` → defensive type check in `FaqManagerPanel`
- #16 `0%` for undefined confidence → shows "New match" when null
- #17 SubmitAnswerForm loading text → "Submitting…" shown when `isPending`
- #18 Skeleton mixed with real data → `FaqManagerPanel` only renders skeletons when `isLoading`
- #19 Navbar `window.location` fallback → removed, only `matchRoute` used
- #20 Incomplete markdown stripping → `FaqCard.stripMarkdown` handles code blocks, links, headings, etc.
- #23 `enabled: false` analytics → removed `enabled` guard, query fires immediately
- #24 `undefined` category as string → defensive nullish access throughout

**3 ⚠️ Still Open (minor):**
- #21 `fetchCategories` is module-level function not a hook (minor)
- #22 "Community Member" fallback — intentional for anonymous answers (minor, keep as-is)
- #25 `admin.faqs.tsx` category cell may still render `[object Object]` — not yet confirmed fixed

## Files Reviewed This Session

All frontend source files in `frontend/src/` read and audited:
- `main.tsx`, `lib/queryClient.ts`, `lib/api.ts`
- `contexts/AuthContext.tsx`
- `hooks/useFaq.ts`, `hooks/useFaqs.ts`, `hooks/useQueryClusters.ts`
- `routes/login.tsx`, `routes/signup.tsx`, `routes/ask.tsx`
- `routes/faqs.tsx`, `routes/faqs.$id.tsx`
- `routes/questions.tsx`, `routes/questions.$id.tsx`
- `routes/admin.tsx`, `routes/admin.queries.tsx`, `routes/admin.faqs.tsx`, `routes/admin.analytics.tsx`
- `components/Navbar.tsx`, `components/SearchBar.tsx`
- `components/CategoryFilter.tsx`, `components/FaqCard.tsx`
- `components/QuestionForm.tsx`, `components/SubmitAnswerForm.tsx`
- `components/AnswerCard.tsx`, `components/AiSuggestionBanner.tsx`
- `components/DocumentStatusCard.tsx`, `components/WelcomeBanner.tsx`
- `components/ProtectedRoute.tsx`
- `components/admin/FaqManagerPanel.tsx`, `components/admin/QueryCard.tsx`
- `components/admin/AnalyticsWidget.tsx`, `components/admin/CategoryCoverageCard.tsx`
- `types/index.ts`

## Decisions Made

- Issue #22 ("Community Member" fallback) is intentional design — when `contributedBy` is a string (ID only, not populated), anonymous answer attribution is correct behavior. Marked as won't-fix.
- Issues #21 and #25: to be resolved as part of the remaining 3 minor frontend fixes before backend work begins.

## Next Steps

1. Fix remaining 3 minor frontend issues (#21, #25, confirm #22 won't-fix) — ✅ DONE
2. Move to backend review — check `backend/CHUNK_ISSUES.md` for known backend issues
3. Begin backend audit to match the thoroughness of the frontend audit done today

## Fixes Applied Today (2026-06-01 afternoon)

**#21 — `fetchCategories` not a proper hook** ✅
- Created `frontend/src/hooks/useCategories.ts` — proper `useQuery` hook wrapping the categories fetch
- Updated `CategoryFilter.tsx` to import and use `useCategories()` instead of inline `useQuery`
- `FRONTEND_ISSUES.md` updated: row marked ✅ Fixed 2026-06-01

**#22 — "Community Member" fallback** 🚫 Won't-fix ✅
- Marked as intentional design in `FRONTEND_ISSUES.md`
- "Community Member" accurately represents anonymous attribution — not a missing field
- `FRONTEND_ISSUES.md` row updated: 🚫 Won't-fix (intentional design)

**#25 — `admin.faqs.tsx` category cell `[object Object]`** ✅
- `FaqManagerPanel.tsx` create-form: `prefill?.category` may be an object from `CategoryCoverageCard` navigation
- Fixed: `typeof prefill?.category === 'string' ? prefill.category : (prefill?.category as any)?.name ?? ''`
- The FAQ table row (`FaqRow`) already had the defensive check (fixed as #15 previously)
- `FRONTEND_ISSUES.md` row updated: ✅ Fixed 2026-06-01

**Final state:**
- All 25 FRONTEND_ISSUES resolved (22 previously + 3 today)
- `npx tsc --noEmit` exit 0 — zero type errors
- Frontend is fully polished and clean

## Files Created/Modified

**Created:**
- `frontend/src/hooks/useCategories.ts` — `useCategories()` hook (replaces module-level `fetchCategories`)

**Modified:**
- `frontend/src/components/CategoryFilter.tsx` — removed module-level `fetchCategories`, now uses `useCategories` hook
- `frontend/src/components/admin/FaqManagerPanel.tsx` — create-form prefill.category defensive check
- `FRONTEND_ISSUES.md` — #21, #22, #25 status rows updated; #22 marked won't-fix
- `memory/FRONTEND_AUDIT_2026-06-01.md` — "All Issues Resolved" section appended

## Notes
- Session used vllm/MiniMaxAI/MiniMax-M2.7 model
- Emie's timezone: GMT+5:30
- Emie prefers one-file-at-a-time execution for chunk work; stop after each file and wait for "continue"